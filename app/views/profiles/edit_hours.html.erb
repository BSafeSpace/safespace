<div class="border-form-div" align="center">
	<h3>Edit Hours of Availability</h3><br>
	<%= simple_form_for @profile do |f| %>
		<%= f.input :counselor_hours, label: false %>
		<%= f.button :submit, 'OK', class: 'btn btn-success' %>
		<%= link_to "Cancel", 'javascript:;', class: 'btn btn-default', id: 'cancel-btn' %>
	<% end %>
</div>