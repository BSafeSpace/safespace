$('.modal').modal()
$('.modal .modal-title').html("<%= @profile.name %>")
$('.modal .modal-body').html("<%= j render(partial: 'profiles/profile_modal_body', locals: {profile: @profile}) %>")
$('.modal .modal-footer').html("<%= j render(partial: 'profiles/profile_modal_footer', locals: {profile: @profile}) %>")
