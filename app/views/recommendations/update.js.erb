console.log("update");
// Remove the recommendation from the table.
<% publish_to '/recommendations/update' do %>
	$("tr[data-rec-id='<%= @recommendation.id.to_s %>']").remove();
	$('.unread-count').text("<%= Recommendation.unopened_count.to_s %>");
<% end %>