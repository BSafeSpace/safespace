<% @rec_user = recommendation.get_user %>
<tr data-rec-id="<%= recommendation.id.to_s %>">
	<td> <%= @rec_user.username %> </td>
	<td width= 60%> <%= recommendation.reason %> </td>
	<!-- <td> <%= recommendation.additional_info %> </td>  -->
	<td> <span data-rec-id="<%= recommendation.id.to_s %>">

<!--  	<%= link_to "Accept Request", conversations_path(sender_id: current_user.id, recipient_id: @rec_user.id), method: 'post', :class => "contact-list-name", remote: true, class: "btn btn-success btn-block start-conversation"%> -->

<!--  	<%= link_to "Accept Request", recommendation_path(recommendation.id), method: 'put', remote: true, class: "btn btn-success btn-block start-conversation", 
			"data-sid" => current_user.id, "data-rip" => @rec_user.id %> -->

			<%= link_to "Accept Request", conversations_path(sender_id: current_user.id, recipient_id: @rec_user.id), method: 'post', :class => "contact-list-name", class: "btn btn-success btn-block start-conversation" %>
				
			</span> 
	</td>
</tr>